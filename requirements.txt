# TubeNote backend/runtime dependencies (CPU-safe).
# Python 3.11 is recommended.
#
# GPU users: also install `requirements-gpu.txt` for the CUDA runtime wheels
# that faster-whisper GPU mode needs. CPU-only machines skip that file, and can
# additionally install the CPU build of torch first (see README > Setup).
# If your PyTorch/OmniVoice setup needs a specific CUDA/MPS/XPU build, install
# the matching torch/torchaudio wheels first, then run this requirements file.

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.7

# Core config/networking
python-dotenv>=1.0
pyyaml>=6.0
requests>=2.31

# LLM and RAG
langchain-core>=0.3
langchain-text-splitters>=0.3
langchain-community>=0.3
langchain-chroma>=0.2
langchain-huggingface>=0.1
sentence-transformers>=3.0
rank-bm25>=0.2

# LLM providers
langchain-openai>=0.2
langchain-google-genai>=2.0
langchain-anthropic>=0.3

# YouTube and ASR
yt-dlp>=2024.8
secretstorage>=3.3; sys_platform == "linux"  # yt-dlp cần để đọc cookie mã hoá bởi keyring khi dùng YT_COOKIES_BROWSER
faster-whisper>=1.0
openai-whisper>=20240930

# Audio/video processing
numpy>=1.26
soundfile>=0.12
ffmpeg-python>=0.2
imageio-ffmpeg>=0.5
audiotsm>=0.1.2
demucs>=4.0
torchcodec

# TTS engines
supertonic>=1.3
omnivoice>=0.1.5
